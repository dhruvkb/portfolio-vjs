function changeDirectoryWalkthrough(dirname) {
    typeText('cd ' + dirname, focus);
}

function concatenateWalkthrough(filename) {
    typeText('cat ' + filename, focus);
}

function listWalkthrough() {
    typeText('ls', focus);
}

function helpWalkthrough() {
    typeText('help', focus);
}